using RevolutionShared.Rose.Data.Equipment;

/// <summary>
/// Weapon database.
/// </summary>
public class WeaponDatabase : ItemDatabase<WeaponData> { } // Those useless and almost empty class / file are a limitation of Unity. We could just generate them like the rest, to bound Data / Database